version: 0.2

phases:
  install:
    runtime-versions:
      php: 8.1
    commands:
      - composer install
  build:
    commands:
      - echo "Build complete"

artifacts:
  files:
    - '**/*'
version: 0.2

phases:
  install:
    runtime-versions:
      dotnet: 6.0
  build:
    commands:
      - dotnet restore
      - dotnet build
      - dotnet publish -c Release -o ./publish

artifacts:
  files:
    - publish/**/*

